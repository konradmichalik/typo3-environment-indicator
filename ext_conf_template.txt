# cat=general/enable/10; type=options[GD=gd,Imagick=imagick,libvips=vips]; label=Intervention Image supports "GD Library", "Imagick" and "libvips" to process images.
general.imageDriver = gd
# cat=general/enable/20; type=boolean; label=Enable the default configuration for the environment indicators. If you want to configure the environment indicators by yourself, set this to false.
general.defaultConfiguration = 1
# cat=frontend/enable/10; type=boolean; label=Enable the favicon generation in frontend context
frontend.favicon = 1
# cat=frontend/enable/20; type=boolean; label=Enable the context frontend hint
frontend.context = 1
# cat=frontend/enable/30; type=boolean; label=Enable the image generation in frontend context
frontend.image = 1
# cat=frontend/enable/40; type=boolean; label=Enable the page title prefix/suffix in frontend context
frontend.pageTitle = 1
# cat=frontend/enable/50; type=boolean; label=Enable the environment HTTP response header in frontend context
frontend.header = 1
# cat=frontend/enable/60; type=boolean; label=Enable the X-Robots-Tag response header in frontend context. Only takes effect where the robots indicator is explicitly registered; it is never part of the default configuration.
frontend.robots = 1
# cat=frontend/enable/70; type=boolean; label=Enable the environment badge in the browser console
frontend.console = 1
# cat=backend/enable/10; type=boolean; label=Enable the favicon generation in backend context
backend.favicon = 1
# cat=backend/enable/20; type=boolean; label=Enable the logo generation in backend context
backend.logo = 1
# cat=backend/enable/30; type=boolean; label=Enable the context item within the backend toolbar
backend.context = 1
# cat=backend/enable/40; type=boolean; label=Enable the backend toolbar item / backend topbar also in production context
backend.contextProduction = 1
# cat=backend/enable/50; type=boolean; label=Enable the backend theme modification (TYPO3 v14+ only, experimental). Colors the entire backend based on the environment.
backend.theme = 0
# cat=backend/enable/60; type=boolean; label=Enable the environment badge on the backend login screen
backend.login = 1
# cat=backend/enable/70; type=boolean; label=Enable the page title prefix/suffix in backend context
backend.pageTitle = 1
# cat=cli/enable/10; type=boolean; label=Enable the CLI banner (opt-in). Prints a colored environment banner to stderr before console commands run in an interactive terminal.
cli.banner = 0
# cat=mail/enable/10; type=boolean; label=Enable the mail subject prefix for outgoing mails (adds the environment prefix to the subject of every mail sent via the TYPO3 Mailer API)
mail.subject = 1
# cat=instance/enable/10; type=string; label=Instance label: Overrides the indicator text of all active indicators for this installation (e.g. "TEST 1"). Leave empty to use the context defaults. Takes precedence over any registered configuration.
instance.label =
# cat=instance/enable/20; type=color; label=Instance color: Overrides the indicator color of all active indicators for this installation. Leave empty to use the context defaults. Takes precedence over any registered configuration.
instance.color =
